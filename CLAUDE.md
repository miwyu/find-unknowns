# Claude Code repository guidance

@AGENTS.md

Use the shared repository instructions in `AGENTS.md` as the source of truth.
The runtime skill has no dependency on a Claude-specific development plugin.

The historical `/skill-creator:skill-creator` evaluation workflow, local plugin
paths, patched tools, and recorded benchmarks are documented in
`docs/legacy-claude-evaluation.md`. Read that file when reproducing historical
runs; it is not a prerequisite for ordinary repository work.

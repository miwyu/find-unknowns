# LunaによるSKILL.md簡略化（2026-09-13）

## 採用結果

日英とも第1候補の本文を採用し、descriptionは元版へ戻した。より短い候補も実行したが、観測した回帰を優先して不採用とした。今回検査した候補内での選択であり、最短性や一般的な性能維持の証明ではない。

| 対象               | 元版の文字数 | 採用版の文字数 | 削減率 |
| ------------------ | -----------: | -------------: | -----: |
| 英語本文           |        7,024 |          5,617 |  20.0% |
| 日本語本文         |        3,158 |          2,574 |  18.5% |
| 英語ファイル全体   |        7,694 |          6,287 |  18.3% |
| 日本語ファイル全体 |        3,414 |          2,830 |  17.1% |

文字数はPythonの`len(str)`、本文はfrontmatterを除いて先頭改行を取り除いた値。トークン数ではない。descriptionは英語627文字、日本語213文字のまま、元版と完全一致する。

段階説明を統合し、四象限の背景解説と重複を削除した。8パターンの参照先、遅延読込、参照先が依存する`Step 4`／`ステップ4`を維持した。参照ファイルと素材、評価定義は変更していない。

## 実験条件

- 起点: `b36726b56653e6a8278393219df38c2a4f2604d1`。開始時の作業ツリーはクリーン。
- 実行者: `gpt-5.6-luna`、reasoning effort `medium`。執筆者もluna（`high`）を使用した。
- 呼出方法: Codexセッションの`collaboration.spawn_agent`、`fork_turns: none`。CLI経由でもネイティブなskill選択でもない。ホストの正確なバージョンと解決済みモデルIDはツール応答に公開されなかった。
- 版・言語ごとに新規実行者。各実行者は独立した3ケースを順に実行するため、ケース間ではコンテキストを共有する。版間の実行者は再利用していない。
- 明示的なskillスナップショットのパス、ユーザーの依頼文、コピーしたfixtureだけを提示。元リポジトリ、採点基準、期待解、他版の結果は読ませない。ユーザー提供のRepository guidanceを優先し、ユーザーの起動契約にある採点チェックリストは実行者に渡さなかった。
- ソース読取とコピーへの限定編集を利用。ネットワーク、配備アクセス、描画、依存追加は使わない条件。case 17のみ指定された計画・本文編集を行う。`RESPONSE.md`は実験の返答記録で、アプリ成果物とは別扱い。
- 親セッションが返答と出力ファイルを意味的に採点。既存assertionを固定し、各ケースの先頭assertionだけを事前に`critical`指定。達成=1、部分的=0.5、未達=0。criticalすべて達成の場合だけ成功○。
- `tool_uses`と`duration_ms`は取得できず、steps・durationは全ケース未取得。再試行回数は実行者の自己申告。

本比較は「現行skill対簡略skill」であり、skillなしとの比較ではない。全21ケース・87項目のベンチマークや、Codex／Claude Codeのホスト対応検証として扱わない。

## Iteration 0 — 現行版

静的にdescriptionと本文の範囲は整合していた。通常例と境界例として、英語の9・12・18、日本語の11・17・20を選択。既存21項目を固定した。

## Iteration 1 — 段階説明と背景の圧縮

本文の重複・背景を削除。以下の6ケースは、各版で同じ言語・条件・順序を使用した。

| シナリオ                 | 言語 |         現行版の精度 |      第1候補の精度 | 約40%削減候補の精度 | critical成功（各版） | retries（各版） |
| ------------------------ | ---- | -------------------: | -----------------: | ------------------: | -------------------- | --------------: |
| 9 実装途中の明示ブレスト | en   |                  75% |                75% |                 75% | ○                    |               0 |
| 12 7挙動の抽出           | en   |                87.5% |               100% |                100% | ○                    |               0 |
| 18 トーン案              | en   |                 100% |               100% |                100% | ○                    |               0 |
| 11 経験的な未知          | ja   |                 100% |               100% |                 75% | ○                    |               0 |
| 17 承認済み計画・実装    | ja   |                 100% |               100% |                100% | ○                    |               0 |
| 20 未保存ノート          | ja   |                 100% |               100% |                100% | ○                    |               0 |
| **項目数で加重した合計** |      | **19.5/21（92.9%）** | **20/21（95.2%）** |  **19/21（90.5%）** | **各6/6**            |                 |

steps・durationはすべて未取得。critical失敗はないが、採否には非criticalを含む回帰も使った。

採点根拠の主な差分:

- case 9: 全版とも6案と固定条件・反応依頼は提示したが、Redisが新たに利用不可となったことによる限定的な再検討の説明が欠落した。
- case 12: 現行版のPOST説明にJSON符号化が明記されず、該当項目を部分的とした。第1候補は`body as JSON`と明記した。
- case 17: 各版とも実ファイルで`message.txt = NEW`と`docs/plan.md`を確認した。
- case 20: 未保存を明示。現行版と約40%削減版は実行者の最終返答を親が記録した。第1候補は実行者が返答記録を保存した。

実行者の不明瞭点は、計画のみの参照契約と明示された同一ターン実装との関係、保存不可タスクと実験用返答記録の関係。後者はskillではなくハーネスの曖昧さ。トーン、対象言語のエラー型、webhookの詳細等の未指定事項はシナリオ側の裁量として分離した。第1候補で新たな重大な本文の不明瞭点は報告されなかった。

## Iteration 2 — descriptionの境界回復と追加短縮

各言語20問、合計40問をdescriptionだけで分類した。期待ラベルを提示せず、実際のタスクは実行させていない。

| description候補                | 正解数 | 誤判定                 |
| ------------------------------ | -----: | ---------------------- |
| 元版・初回                     |  40/40 | なし                   |
| 第1候補の抽象化したdescription |  36/40 | 14・16、日英とも偽陽性 |
| 具体的な自己向け依頼例を復元   |  38/40 | 16、日英とも偽陽性     |
| 元版・新規実行者で反復         |  38/40 | 16、日英とも偽陽性     |
| 約30%短縮した別候補・初回      |  38/40 | 16、日英とも偽陽性     |
| 同候補・新規実行者で反復       |  38/40 | 16、日英とも偽陽性     |

「発見パターン」という抽象化で、ユーザー調査の質問作成まで含む解釈が出た。具体的な依頼例の復元でこの誤判定は解消したが、複雑で未指定事項のある機能依頼をどう扱うかは元版でも揺れた。失敗した個々のクエリを禁止例へ追加する変更や、正解ラベルの変更は行っていない。

元版2回が78/80、最短候補2回が76/80。統計的な劣化を断定できる規模ではないが、性能維持を優先しdescription短縮は採用しなかった。これはネイティブdiscoveryの観測ではなく、description分類の補助検査である。

## Iteration 3 — 約40%の本文圧縮（不採用）

ルーティング条件と参照パスを統合し、参照先と重複する件数・節紹介も削除した。実行前に「他者向け説明」「新証拠による限定再検討」「複合質問の禁止」「計画手順は追加判断でない」を復元した。

case 11で、合意済みのクライアント単位を再決定する表現と、fail-openを`通すのか（カウント欠落を許容するのか）`と扱う表現が出た。親の採点で「合意済み選択を再検討しない」が未達となった。全体90.5%は元版92.9%を下回るため、この圧縮版は採用しなかった。

未使用の英語case 10（マージ後の盲点調査）も追加した。実装者・執筆者への修正材料には使っていない。5つのソース根拠付き発見と未検証事項、具体的確認を提示し、4/4項目達成。ただしこれは**不採用の約40%削減版**での結果で、採用本文のhold-out成功には算入しない。専用の新規エージェント起動が`agent thread limit reached`で失敗したため、最終英語実行者の4番目の独立タスクとして実行した。実行者はパス誤記で読取コマンドを1回再試行したが、判断の再試行は0と報告した。

## 採用と停止条件

第1候補の本文と元版descriptionを組み合わせて採用した。本文は評価済み第1候補と完全一致、descriptionは元版と完全一致する。この**最終組合せそのものの新規エージェント再実行は未実施**であり、表の95.2%は第1候補の実行結果を指す。

新規エージェント上限に達し、steps・durationも未取得のため、連続2回とhold-outを含む厳密な収束条件は満たしていない。リソース打ち切りとして、回帰を観測した版を戻し、検証済みの変更を採用した。最小性、全ケースの性能維持、native discovery、両ホストの実動作は未検証。

## 構造検査と記録

- `python3 tools/evaluate.py validate`: 21ケース・87項目と日英ツリーの検証を通過。
- `python3 -m unittest discover -s tests -v`: 22テスト通過。
- `git diff --check`: 通過。
- pristine fixtureのSHA-256一致を確認。依存追加・fixtureの修正なし。

これらは構造・ツール検査であり、モデルの行動結果とは別である。行動採点とdescription分類はJSONの証拠を個別に確認して集計した。CLI runner用の出力形式ではないため、`tools/evaluate.py aggregate`のベンチマークと偽っていない。

ローカルの完全記録は`find-unknowns-workspace/luna-simplification-2026-09-13/`に保持する（ignored、配布依存ではない）。skillスナップショット、TASK/RESPONSE、コピーの成果物、固定採点契約、項目別採点証拠、自己申告、分類結果、集計、ハッシュを含む。`final/`は実行時の名前で**不採用の約40%削減版**、`accepted/`が採用版。ツールの完全な内部トレースや未公開usage metadataを保存したという意味ではない。
